{
  "name": "tool-system",
  "version": "0.1.0",
  "private": true,
  "description": "Headless, agent-first runtime for declared, brokered, policy-gated capabilities.",
  "type": "module",
  "exports": { ".": { "types": "./dist/src/index.d.ts", "import": "./dist/src/index.js" } },
  "bin": { "tool-system": "./dist/cli/main.js" },
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "verify": "npm run typecheck && npm test && npm run build && node dist/cli/main.js health --json"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
